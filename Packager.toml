# Packager.toml

name           = "snap"
product-name   = "Snap"
identifier     = "io.github.glooring.snap"
version        = "7.2.0"
out-dir        = "dist"

before-packaging-command = "cargo build --release"
targets = ["nsis", "msi"]
