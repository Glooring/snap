# Local Reference Inputs

This folder is for local, ignored copies of source material used while planning the Snap OSS refactor.

It intentionally ignores copied roadmap/workflow files so the public Snap repository does not depend on private absolute paths from another project.

Expected local copies:

- `ROADMAP_SNAP_CODEX_OSS.md`
- `refactor-workflow.md`

Committed docs under `docs/architecture-audit/` must stay self-contained. Do not make future plans depend on these ignored copies being present.
